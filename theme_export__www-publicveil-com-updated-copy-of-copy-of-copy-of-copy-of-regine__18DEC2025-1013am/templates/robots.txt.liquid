{%- comment -%}
  Shopify default robots.txt rules — do not remove!
{%- endcomment -%}
{% for group in robots.default_groups %}
  {{- group.user_agent -}}
  {% for rule in group.rules %}
    {{- rule -}}
  {% endfor %}
  {%- if group.sitemap != blank -%}
    {{ group.sitemap }}
  {%- endif -%}
{% endfor %}

{%- comment -%}
  Explicitly allow ChatGPT’s crawler (OAI-SearchBot)
{%- endcomment -%}
User-agent: OAI-SearchBot
Allow: /
